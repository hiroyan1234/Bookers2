<div class="container px-5 px-sm-0">
  <div class="row">
    <%= render 'list', user:@user, book:@book %>
    <% @users.each do |user| %>
    <div>
      <%= user.name %>
    </div>
    <% end %>
  </div>
</div>
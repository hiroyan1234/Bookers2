<h1>Editing Book</h1>

<%= form_with model: @book do |f| %>
  <%= f.label :title, "Title" %>
  <%= f.text_field :title %>
  
  <%= f.label :opinion, "Opinion" %>
  <%= f.text_area :opinion %>

  <%= f.submit "Update Book" %>
<% end %>
